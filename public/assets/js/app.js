// Agenda de Cobranza - Imperio Comercial
// JS propio, sin build step. La interactividad de cada vista (tabs,
// toggles, modales) se maneja con Alpine.js directamente en el HTML
// (x-data/x-show/etc.) - este archivo queda para lógica que no encaje
// ahí, como el fetch() de registro de gestión al hacer clic en WhatsApp
// (se agrega en Fase 2).
